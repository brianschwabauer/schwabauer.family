{
	"name": "schwabauerfamily",
	"version": "0.0.1",
	"private": true,
	"description": "schwabauer.family — a tiny private site for our family. Built on Cloudflare Workers + SvelteKit + delightstack.",
	"type": "module",
	"scripts": {
		"dev": "pnpm -C app dev",
		"build": "pnpm -C app build",
		"deploy": "pnpm -C app deploy",
		"check": "pnpm -C app check",
		"format": "pnpm -C app format"
	},
	"engines": {
		"node": ">=20"
	},
	"packageManager": "pnpm@10.18.0"
}
